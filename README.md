# Паникёр 3000

Скринер для обнаружения паники/жадности на MOEX.

## 📦 Быстрый старт

1. Скопируйте `.env.example` в `.env`
   ```bash
   cp .env.example .env

2. Заполните токены в файле .env

3. Установите зависимости:
bash

pip install -r requirements.txt

4. Запустите сканер:
bash

python run_scanner.py

📁 Структура проекта

    core/ — ядро логики

    data/ — работа с данными

    bot/ — Telegram-бот

    dashboard/ — Streamlit-дашборд

    config/ — файлы конфигурации

    tests/ — тесты

🔧 Требования

    Python 3.9+

    Токен Tinkoff API

    Токен Telegram бота